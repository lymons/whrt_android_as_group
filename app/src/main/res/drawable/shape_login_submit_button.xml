<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <solid android:color="@color/login_color" />
    <!-- 渐变 -->
    <!-- gradient android:angle="270" android:endColor="#C6E2FF" android:startColor="#E2C13F" / -->
    <!-- 圆角 -->
    <corners android:radius="2dp" />
    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="#ffffff" />
    <!-- 圆角 android:radius为角的弧度，值越大角越圆 -->
    <!-- 若想分别实现四个角圆角弧度 -->
    <!-- 右上角="" 右下角="" 左上角="" 左下角="" -->
    <corners
        android:bottomLeftRadius="3dip"
        android:bottomRightRadius="3dip"
        android:topLeftRadius="3dip"
        android:topRightRadius="3dip" />

</shape>